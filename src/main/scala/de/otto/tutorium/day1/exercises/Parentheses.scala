package de.otto.tutorium.day1.exercises

object Parentheses {
  /**
    * Die Function soll testen, ob eine Liste von Zeichen richtig geklammert ist.
    * Für jede öffnende Klammer muss es genau eine schließende Klammer geben und umgekehrt.
    * Beispiel: (Dieser (kleine) Test ist richtig.)
    * Hinweis: Mit <code>chars.head</code> liefert das erste Element der Liste zu.
    * Mit <code>chars.tail</code> liefert die Liste ohne das erste Element
    * Mit <code>chars.isEmpty</code> kann man prüfen ob eine Liste leer ist.
    *
    * @param chars List that should be checked, if its Parentheses are balanced.
    */
  def isWellFormed(chars: List[Char]): Boolean = ???
}
